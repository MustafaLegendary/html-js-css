function myFunction() {
    const kenar = prompt("Karenin bir kenar uzunluğunu giriniz.");
    const cevreFormülü = 4 * kenar;
    const alanFormülü =  Math.pow(kenar, 2);
    alanFormülü = 5
    alert("Cevresi: " + cevreFormülü +"  "+ "Alanı: " + alanFormülü)
}
